
----------------------------------------------------------------
-- FILE: HelixConfig
----------------------------------------------------------------

local HelixConfig = {
    TickRate = 60,
    MaxTickRate = 240,
    MinTickRate = 15,
    MaxTicksPerFrame = 3,
}

return HelixConfig
